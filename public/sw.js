// AUTO-GENERATED BY BUILD.JS - DO NOT EDIT DIRECTLY
const CACHE_NAME = 'neuron-iq-cache-1780738253034';
const ASSETS = [
  "",
  "/",
  "adding-and-subtracting-integers.html",
  "algebra.html",
  "app.js",
  "artificial-intelligence.html",
  "classical-mechanics.html",
  "computer-science-cs.html",
  "computer-science.html",
  "dimensions.html",
  "elementary-algebra.html",
  "experimental-physics.html",
  "exponents-and-square-roots.html",
  "fermi-problems.html",
  "fractions.html",
  "global.js",
  "graph.js",
  "gravity.html",
  "icon.svg",
  "index.html",
  "international-system-of-units.html",
  "linear-algebra.html",
  "manifest.json",
  "mathematical-tools-basics.html",
  "mathematics.html",
  "multiplying-and-dividing-integers.html",
  "order-of-operations-pemdas-bodmas.html",
  "order-of-operations.html",
  "page.css",
  "perceptrons.html",
  "physics.html",
  "problem-solving-and-estimation.html",
  "problem-solving-and-estimations.html",
  "progression.html",
  "quantum-entanglement.html",
  "quantum-mechanics.html",
  "real-numbers-and-their-operations.html",
  "router.js",
  "shared.css",
  "sitemap.html",
  "style.css",
  "the-speed-of-light.html",
  "units-and-dimensions.html",
  "vectors-and-scalars.html"
];

self.addEventListener('install', (e) => {
    e.waitUntil(
        caches.open(CACHE_NAME).then((cache) => {
            console.log('[Service Worker] Pre-caching offline assets');
            return cache.addAll(ASSETS);
        }).then(() => self.skipWaiting())
    );
});

self.addEventListener('activate', (e) => {
    e.waitUntil(
        caches.keys().then((keys) => {
            return Promise.all(
                keys.map((key) => {
                    if (key !== CACHE_NAME) {
                        console.log('[Service Worker] Cleaning old cache:', key);
                        return caches.delete(key);
                    }
                })
            );
        }).then(() => self.clients.claim())
    );
});

self.addEventListener('fetch', (e) => {
    const url = new URL(e.request.url);
    
    // Cache CDNs dynamically on first request
    if (url.origin !== self.location.origin) {
        if (url.hostname.includes('jsdelivr.net') || url.hostname.includes('googleapis.com') || url.hostname.includes('gstatic.com')) {
            e.respondWith(
                caches.match(e.request).then((cachedResponse) => {
                    if (cachedResponse) return cachedResponse;
                    return fetch(e.request).then((networkResponse) => {
                        if (networkResponse && networkResponse.status === 200) {
                            const responseToCache = networkResponse.clone();
                            caches.open(CACHE_NAME).then((cache) => {
                                cache.put(e.request, responseToCache);
                            });
                        }
                        return networkResponse;
                    }).catch(() => {
                        // Offline return nothing if network fails
                    });
                })
            );
            return;
        }
    }
    
    // Cache-first for local assets
    e.respondWith(
        caches.match(e.request).then((cachedResponse) => {
            if (cachedResponse) {
                return cachedResponse;
            }
            return fetch(e.request).then((networkResponse) => {
                if (networkResponse && networkResponse.status === 200) {
                    const responseToCache = networkResponse.clone();
                    caches.open(CACHE_NAME).then((cache) => {
                        cache.put(e.request, responseToCache);
                    });
                }
                return networkResponse;
            });
        }).catch(() => {
            if (e.request.mode === 'navigate') {
                return caches.match('index.html');
            }
        })
    );
});